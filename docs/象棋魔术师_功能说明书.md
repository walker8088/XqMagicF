# 象棋魔术师 (XQMagic) 功能说明书

> 版本：1.26.1 | 更新日期：2026-05-24

---

## 一、产品概述

**象棋魔术师（XQMagic）** 是一款运行于 Windows 平台的免费中国象棋桌面软件，采用 Python + PyQt5 开发。核心定位是 **打谱研究、残局练习、人机对抗与局面分析**。

| 项目 | 信息 |
|------|------|
| 当前版本 | `1.26.1` |
| 运行环境 | Windows 8+（Python ≥ 3.11） |
| 开源引擎 | Pikafish（皮卡鱼）为主要测试引擎 |
| 协议支持 | UCI / UCCI 引擎协议 |

---

## 二、五大核心模式

| 模式 | 名称 | 功能描述 |
|------|------|----------|
| `Free` | **自由练习** | 从任意局面自由走子，云库/引擎实时评估着法质量，棋谱可保存到个人数据库 |
| `EngineAssit` | **引擎辅助** | 引擎在后台实时分析当前局面，在棋盘上显示箭头提示最佳着法 |
| `EngineFight` | **人机对战** | 引擎扮演红方或黑方，与人类玩家对抗；局后可逐着复盘评分，标出劣着与推荐着法 |
| `EngineEndGame` | **杀法挑战** | 内置多套杀局集（梦入神机、适情雅趣 360 局等），红先胜排局，由简到难挑战，完成后自动进入下一局 |
| `EngineOnline` | **连线分析** | 连接云端棋库（chessdb.cn），实时获取局面最佳着法与评分 |

---

## 三、复盘功能

| 复盘方式 | 说明 |
|----------|------|
| **云库复盘** | 逐着对比云库推荐着法，标注好棋★/一般✓/劣着✗ |
| **引擎复盘** | 引擎对已有棋谱重新分析，逐着打分并指出改进方案 |

> 支持从棋谱起始局面或任意中间步骤开始复盘。

---

## 四、棋谱与开局库

### 4.1 支持的棋谱格式

| 操作 | 支持格式 |
|------|----------|
| **读取** | `.xqf`（象棋演播室）、`.cbr` / `.cbl`（象棋桥）、`.pgn`（通用格式） |
| **写入** | `.pgn`、`.xqf` |

### 4.2 开局库支持

| 类型 | 格式 | 说明 |
|------|------|------|
| 勇芳格式 | `.yfk` | 优先使用 `openbook.yfk` |
| 鹏飞格式 | `.pfbook` | 备用格式 |
| 大师对局 | 内置（`Books/`） | 标记 `D` |
| 古典对局 | 内置（`Books/`） | 标记 `G` |
| 个人棋谱 | 用户保存 | 标记 `*` |

### 4.3 ECCO 开局识别
- 自动识别当前局面所属的 **ECCO 开局分类编码**，在着法列表中显示。

---

## 五、棋盘与交互

| 功能 | 说明 |
|------|------|
| **棋盘换肤** | 支持自定义棋盘与棋子皮肤（`Skins/` 目录） |
| **棋盘缩放** | 缩放比例 0.5x ~ 最大 |
| **图片识别** | 支持彩色棋盘截图识别为 FEN 局面 |
| **复制棋局** | 复制局面为 FEN 文本或图片到剪贴板 |
| **拖放打开** | 棋谱/库文件可直接拖放到窗口打开 |
| **右键提示** | 右键点击棋盘显示下一步最优着法 |
| **局面编辑** | 可视化编辑棋盘局面，支持当前皮肤预览 |

---

## 六、引擎系统

| 特性 | 说明 |
|------|------|
| **协议** | UCI / UCCI |
| **分析模式** | 精准模式（deep）/ 快速模式（quick）/ 挑战模式（fight） |
| **MultiPV** | 支持多路分析（可调 PV 数量），以分支树形式展示 |
| **引擎设置** | 统一对话框管理（深度、时间、线程数、哈希表大小等） |
| **优先级模式** | 云库优先 / 引擎优先，可切换 |
| **局面分析** | 勾选"局面分析"后，引擎结果以不同方式呈现 |

---

## 七、云端数据库

| 功能 | 说明 |
|------|------|
| **实时查询** | 连接 chessdb.cn，自动搜索当前局面最佳着法 |
| **着法评分** | 返回着法列表含分数、偏离度、中文着法文本 |
| **本地缓存** | LRU 缓存策略（最大 10,000 局面），减少重复查询 |
| **云库复盘** | 对整局棋逐着查询云库，生成着法质量标注 |

---

## 八、本地数据库

| 数据库 | 格式 | 用途 |
|--------|------|------|
| `masterbook.db` | SQLite (Peewee) | 大师开局库 |
| `localbook.db` | SQLite (Peewee) | 个人收藏棋谱/局面（Book + Bookmark 表） |
| `endbooks.json` | TinyDB | 残局练习收藏 |
| `endbook.db` | SQLite | 残局库 |

---

## 九、着法质量标注

| 图标 | 含义 | 偏离阈值（diff） |
|------|------|-----------------|
| （无标记） | 最优着法 | ≥ -5 |
| ★ | 好棋 | ≥ -30 |
| ✓ | 一般 | ≥ -70 |
| ✗ | 劣着 | ≥ -100 |

---

## 十、窗口与面板布局

```
┌─────────────────────────────────────────────────────────┐
│  菜单栏：文件 | 对局 | 显示 | 系统                        │
├──────────┬──────────────────────────┬───────────────────┤
│  左面板   │                          │   右面板          │
│  ┌─────┐ │                          │  ┌─────────────┐ │
│  │开局  │ │                          │  │ 着法列表     │ │
│  │备选  │ │     棋盘面板              │  │ (分数/标注)  │ │
│  │着法  │ │                          │  ├─────────────┤ │
│  │残局  │ │                          │  │ 注解面板     │ │
│  │收藏  │ │                          │  ├─────────────┤ │
│  │棋谱  │ │                          │  │ 分支面板     │ │
│  │库    │ │                          │  └─────────────┘ │
│  └─────┘ │                          │                   │
├──────────┴──────────────────────────┴───────────────────┤
│  底部面板：引擎控制（模式/MultiPV/深度/时间/线程/哈希）   │
├─────────────────────────────────────────────────────────┤
│  状态栏：模式 | 局面 | 引擎状态                            │
└─────────────────────────────────────────────────────────┘
```

### 停靠面板明细

| 面板 | 区域 | 用途 |
|------|------|------|
| `HistoryWidget` | 右 | 着法列表，含分数显示、注解面板、分支面板 |
| `EngineWidget` | 下 | 引擎控制面板，MultiPV 分析树 |
| `BoardActionsWidget` | 左 | 开局备选着法面板（"搜索云库"开关 + 着法列表） |
| `EndBookWidget` | 左 | 残局练习库 |
| `BookmarkWidget` | 左 | 收藏的局面/棋谱 |
| `GameLibWidget` | 左 | 棋谱库浏览器 |

### 工具栏

| 工具栏 | 功能 |
|--------|------|
| **文件** | 打开、保存、收藏 |
| **对局** | 自由练习、人机对战、杀法挑战、重新开始、编辑局面 |
| **显示** | （预留） |
| **系统** | 退出 |

---

## 十一、设置与持久化

| 存储方式 | 内容 |
|----------|------|
| `QSettings` (注册表) | 窗口位置/大小、面板分割比例、音效音量、棋盘皮肤、最后打开目录 |
| `XQMagic.ini` | 主引擎配置（引擎类型、引擎路径） |
| 状态恢复 | 关闭时保存，下次启动自动恢复窗口状态与面板布局 |

---

## 十二、技术架构概览

```
XQMagic.py (入口 5 行)
  └─ XQMagicUI/App.py
       └─ MainWindow (中央调度器 ~1918 行)
            ├─ BoardWidgets.py     → 棋盘渲染
            ├─ Widgets.py          → 所有停靠面板
            ├─ Engine.py           → UCI/UCCI 引擎管理（后台线程）
            ├─ CloudDB.py          → 云端棋库客户端
            ├─ LocalDB.py          → 本地 SQLite 数据库
            ├─ Storage.py          → TinyDB 持久化
            ├─ Online.py           → 屏幕截图 + 棋盘识别
            ├─ Cache.py            → LRU 局面缓存管理器
            ├─ Ecco.py             → ECCO 开局百科 DLL
            └─ Globl.py            → 全局状态 (fenCache, 单例, 窗口引用)
```

---

## 十三、数据流

### 13.1 走子执行流

```
用户点击棋盘
  └─> ChessBoardWidget.mousePressEvent()
        └─> try_move() 验证合法性
              └─> tryMoveSignal.emit(from, to)
                    └─> MainWindow.onTryBoardMove()
                          └─> onMoveGo(iccs)
                                ├─ board.move_iccs() 执行走子
                                ├─ 创建位置字典，追加到 positionList
                                ├─ 更新 fenCache[new_fen]
                                ├─ historyView.onNewPostion()  添加着法行
                                ├─ updateStatus()  音效、状态栏
                                └─ changePositionSignal.emit()
                                      └─ onChangePosition()
                                            ├─ boardView.showMove()
                                            ├─ localSearch()  查询开局库
                                            ├─ cloudQuery.startQuery()  云库模式
                                            └─ runEngine()  引擎分析
```

### 13.2 引擎输出流

```
EngineManager._runOnce()
  ├─ 'bestmove' -> moveBestSignal -> MainWindow.onTryEngineMove()
  │     ├─ updateFenCache(fenInfo, isEngine=True)
  │     ├─ 如果是残局 -> onMoveGo(iccs) 自动走子
  │     └─ 否则 -> showBestHint()  棋盘箭头提示
  └─ 'info_move' -> moveInfoSignal -> engineView.onEngineMoveInfo()
        └─ 更新 MultiPV 分析树显示
```

### 13.3 云库查询流

```
CloudDB.startQuery(position)
  ├─ 检查 move_cache（已缓存则跳过）
  └─ 创建 NetQuery -> HTTP GET chessdb.cn
        └─ onQueryFinished()
              ├─ 解析管道分隔响应
              ├─ 分数转换为红方视角
              ├─ 计算与最佳着法的偏离度
              ├─ updateCache() -> Globl.fenCache
              └─ query_result_signal.emit()
                    └─ MainWindow.onCloudQueryResult()
                          ├─ updateFenCache(query)
                          ├─ 与引擎分析合并
                          └─ actionsView.updateActions()
```

---

## 十四、版本更新亮点

| 版本 | 重点更新 |
|------|----------|
| 1.26.1 | 引擎分支双击查看招法、快速/精准分析模式、彩色图片识别 |
| 1.25.1 | 局面编辑支持皮肤 |
| 1.24.4 | 任意步骤走变招、文件拖放、棋盘换肤、ECCO开局显示、cbr/cbl 格式支持 |
| 1.24.3 | 右键最优着法提示、云库/引擎优先模式、引擎复盘、PGN 读写 |
| 1.24.2 | 人机练习模式、杀法自动下一局、云库复盘 |

---

## 十五、快速启动

```bash
# 启动应用
python XQMagic.py

# 调试模式
python XQMagic.py --debug

# 清除设置
python XQMagic.py --clean

# 打开棋谱文件
python XQMagic.py game.pgn
```

---

## 十六、常见问题

### 引擎无法启动
- 检查 `XQMagic.ini` 中的引擎路径是否正确
- 确认引擎文件有执行权限
- 检查引擎依赖是否完整

### 数据库错误
- 确认 `Game/` 目录下的数据库文件存在
- 检查文件读写权限
- 尝试使用 `--clean` 清除设置后重启

### 云库查询超时
- 检查网络连接
- 云库服务器可能暂时不可用，切换为引擎优先模式

---

*本文档由项目代码自动生成并人工校对，如有遗漏请查阅源码。*
